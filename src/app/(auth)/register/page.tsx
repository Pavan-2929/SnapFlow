import { Metadata } from "next";
import React from "react";

export const metadata: Metadata = {
  title: "Register",
};

const RegisterPage = () => {
  return <div className="flex h-screen items-center justify-center p-5"></div>;
};

export default RegisterPage;
